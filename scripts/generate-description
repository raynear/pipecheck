#!/bin/bash

# Generate Description CLI wrapper script
# Usage: ./scripts/generate-description [options]

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$SCRIPT_DIR/.."
CLI_DIR="$PROJECT_ROOT/tools/cli"

cd "$PROJECT_ROOT"
dart run "$CLI_DIR/bin/generate_description.dart" "$@"
