#!/bin/bash

# Screenshot CLI wrapper script
# Usage: ./scripts/screenshot [options]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_ROOT"
dart run tools/cli/bin/screenshot.dart "$@"
