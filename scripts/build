#!/bin/bash

# Build CLI wrapper script
# Usage: ./scripts/build [options] (or ./build via symlink)

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$SCRIPT_DIR/.."
CLI_DIR="$PROJECT_ROOT/tools/cli"

cd "$PROJECT_ROOT"
dart run "$CLI_DIR/bin/build.dart" "$@"
