name: bp_launcher
publish_to: none

environment:
  sdk: ^3.0.0

# ref는 project.yaml tooling.cli_ref와 **같이** 올린다 (절차: boiler_plate §4.5).
dependencies:
  boilerplate_cli:
    git:
      url: https://github.com/raynear/boiler_plate.git
      ref: cli-v1.3.9
      path: tools/cli
