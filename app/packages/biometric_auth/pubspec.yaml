name: biometric_auth
description: Biometric / device authentication (local_auth wrapper) for Flutter apps (app-agnostic, opt-in).
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  utils:
    path: ../utils
  local_auth: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0          # workspace 단일 해석 — app과 정렬 (P2-19c)

flutter:
